extends layout

block content
	h1 #{roomName}
	p sid:
		span#sidSpan #{sid}

	a.btn.btn-default.buttonSpace(href="http://player.azk.dev/?sid=#{sid}", target="player-window") Player
	a.btn.btn-default.buttonSpace(href="http://searcher.azk.dev/?sid=#{sid}", target="search-window") Search for music

	br
	br
	h4 Who is connected
	div.clients

script(src='/socket.io/socket.io.js')
