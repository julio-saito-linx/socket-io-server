extends layout

block content
    h1 Where are you?
    form(role='form',method='POST')
        .form-group
          label(for='roomName') Room name:
          input#roomName.form-control(type='text', placeholder='Kitchen or Living Room or Anything else', name='roomName')
        button.btn.btn-default(type='submit') Submit
